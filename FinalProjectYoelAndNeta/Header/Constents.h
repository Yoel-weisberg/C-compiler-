#pragma once
#include <vector>
#include <iostream>

enum Tokens_type {
	INT,
	LPAREN,
	RPAREN,
	ADDITION,
	MULTIPLICATION,
	DIVISION,
	SUBTRACTION,
	TYPE_DECLERATION,
	EQUEL_SIGN,
	SEMICOLUMN,
	IDENTIFIER,
	IF_WORD,
	AND,
	OR,
	L_CURLY_PRAN,
	R_CURLY_PRAN,
	ELSE, 
	LOWER_THEN,
	HIGHER_THEN,
	EQUELS_CMP
};
